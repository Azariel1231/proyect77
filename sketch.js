console.log("Mi primer proyecto en la consola");

            var primer_nombre = "Azariel";
            var apellido = "carrasco";

            var nombre_completo = primer_nombre.concat(apellido);
            console.log(nombre_completo);

            var a = 100;
            var b = 50;

            var sumar = a + b ;
            console.log(sumar);

            var restar = a - b ;
            console.log(restar);

            var multiplicar = a * b ;
            console.log(multiplicar);

            var dividir = a / b ;
            console.log(dividir);
